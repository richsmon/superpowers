---
description: "Brain workflow — Figma → brain reverse snapshot via REST API (read-only on Figma, additive JSON+PNG capture, commits direct to main NOT a branch)."
---

Invoke **brain-figma-snapshot** + **token-efficient-communication** skills. Load and follow `skills/brain-figma-snapshot/SKILL.md` exactly — Git Safety Protocol, **Commit Policy (direct to main, NEVER a branch+PR)**, **signed commit required for every per-substep commit (no signature → no commit, no bypass)**, **PAT from `<brain>/.env`** via `set -a; source .env; set +a`, then execute Phases 0 → 4 from `meta/figma-to-brain-snapshot-workflow.md` in the brain repo (the brain doc is the canonical procedure; this skill is the policy + invocation wrapper). Snapshot is read-only on Figma — no `use_figma` writes. Apply `token-efficient-communication` to all final output. If your environment exposes a Skill tool, load both before acting.
