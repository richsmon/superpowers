---
description: "Brain workflow — draft features/{name}/spec.md in the brain repo"
---

Invoke **write-feature-spec** + **token-efficient-communication** skills. Load and follow `skills/write-feature-spec/SKILL.md` exactly (Locate Brain Repo, Process, Template, commit policy). Apply `token-efficient-communication` to all final output to human partner. If your environment exposes a Skill tool, load both before acting.
