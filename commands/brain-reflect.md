---
description: "Brain workflow — capture learnings from completed work into atomic notes / decisions / entity updates / follow-ups"
---

Invoke **brain-reflect** + **token-efficient-communication** skills. Load and follow `skills/brain-reflect/SKILL.md` exactly (Locate Brain Repo, identify reflected work, triage takeaways via routing table, write each piece in the right place, update session note). Delegate to `log-decision` and `log-blocker` where appropriate — never write decisions directly. Apply `token-efficient-communication` to all final output. If your environment exposes a Skill tool, load both before acting.
