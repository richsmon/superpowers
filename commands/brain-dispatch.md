---
description: "Brain workflow — dispatch a coding agent (subagent or Paperclip) to a workspace's linked code repo with brain context bundled"
---

Invoke **brain-dispatch** + **token-efficient-communication** skills. Load and follow `skills/brain-dispatch/SKILL.md` exactly (Locate Brain Repo, identify workspace + linked repo, bundle context, choose mode, dispatch, reflect results into session). Apply `token-efficient-communication` to all final output. If your environment exposes a Skill tool, load both before acting.
