---
description: "Brain workflow — recommend the next concrete task to pick across active workspaces (read-only orientation)"
---

Invoke **brain-next** + **token-efficient-communication** skills. Load and follow `skills/brain-next/SKILL.md` exactly (Locate Brain Repo, scan workspaces with active phases, score candidates by status priority, surface ONE recommendation + context preview that `brain-dispatch --task T-NN` would auto-bundle, plus blocked / waiting-on / other-workspaces footnotes). Apply `token-efficient-communication` to all final output. STRICTLY read-only — never write to any file. If your environment exposes a Skill tool, load both before acting.
