---
description: "Brain workflow — create features/{feature}/ with spec.md and plan.md stub inside an existing workspace"
---

Invoke **brain-new-feature** + **token-efficient-communication** skills. Load and follow `skills/brain-new-feature/SKILL.md` exactly (Locate Brain Repo, optionally chain `brainstorming` + `write-feature-spec`, scaffold feature folder, update entity, suggest `writing-plans` next). Apply `token-efficient-communication` to all final output. If your environment exposes a Skill tool, load both before acting.
