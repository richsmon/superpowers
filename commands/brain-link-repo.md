---
description: "Brain workflow — link a code repository to a brain workspace (writes repo: block in graph/entities/{name}.yaml)"
---

Invoke **brain-link-repo** + **token-efficient-communication** skills. Load and follow `skills/brain-link-repo/SKILL.md` exactly (Locate Brain Repo, verify workspace + git path, write `repo:` block, update REPO_KNOWLEDGE.md). Apply `token-efficient-communication` to all final output. If your environment exposes a Skill tool, load both before acting.
