---
description: "Brain workflow — print one-screen overview of brain (active workspaces, linked repos, today's session, recent decisions)"
---

Invoke **brain-status** + **token-efficient-communication** skills. Load and follow `skills/brain-status/SKILL.md` exactly (Locate Brain Repo, enumerate workspaces + entities + sessions + decisions, print compact one-screen status). Apply `token-efficient-communication` to all final output. Read-only — never modify the brain. If your environment exposes a Skill tool, load both before acting.
