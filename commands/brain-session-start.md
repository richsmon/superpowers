---
description: "Brain workflow — start daily session file (no commit until session-end)"
---

Invoke **session-start** + **token-efficient-communication** skills. Load and follow `skills/session-start/SKILL.md` exactly (Locate Brain Repo, github-username path, no auto-commit). Apply `token-efficient-communication` to all final output. If your environment exposes a Skill tool, load both before acting.
