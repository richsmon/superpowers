---
description: "Brain workflow — quick mid-work commit: detect state, fill session/Next gaps, propose message, commit locally (no push)"
---

Invoke **brain-checkpoint** + **token-efficient-communication** skills. Load and follow `skills/brain-checkpoint/SKILL.md` exactly (Locate Brain Repo, snapshot state, identify gaps via the gap table, show diff summary, propose commit message with proper type prefix, confirm + commit locally — never push). Apply `token-efficient-communication` to all final output. If your environment exposes a Skill tool, load both before acting.
